form( action='/users/update', method='post' )
  input(type='hidden', name='_method', value='PUT')
  div
    label( for='name' ) full name
    input( name='user[name]', value='#{name}' )
  div
    label( for='login' ) login
    input( name='user[login]', value='#{login}', readonly='true' )
  - if(user.attribution == 'google')
    div.message
      p No need to add a password since you login with google.
  div
    label( for='password' ) password
    input( name='user[password]' )
  div
    label( for='tracker' ) Tracker API Key
    input( name='user[tracker_id]', value='#{tracker}' )
  div.submit
    input( type='submit' )
