var Projects = require( __dirname + '/../../app/controllers/projects_controller.js')
  , should   = require('should');

describe('Project', function(){
  it('should retrive a list of users projects when a key is present',function(){
  });
});
